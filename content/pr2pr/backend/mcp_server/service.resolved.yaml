apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: search-toolbox
  annotations:
    run.googleapis.com/launch-stage: BETA
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/execution-environment: gen2
        run.googleapis.com/network-interfaces: '[{"network":"search-demo-vpc","subnetwork":"search-demo-vpc"}]'
        run.googleapis.com/vpc-access-egress: private-ranges-only
    spec:
      serviceAccountName: search-backend-sa@prop-search-demo.iam.gserviceaccount.com
      containers:
      - name: toolbox
        image: europe-west3-docker.pkg.dev/prop-search-demo/search-app-repo/search-toolbox:20260407-153534
        ports:
        - containerPort: 8080
        env:
        - name: DB_PASSWORD
          value: "c29nr823u-?48dn#99"
      
      # --- ALLOYDB AUTH PROXY SIDECAR ---
      - name: auth-proxy
        image: gcr.io/alloydb-connectors/alloydb-auth-proxy:latest
        args:
        - "projects/prop-search-demo/locations/europe-west3/clusters/search-cluster/instances/search-primary"
        - "--address=127.0.0.1"
        - "--port=5432"
        resources:
          limits:
            cpu: "1000m"
            memory: "512Mi"
